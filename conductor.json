{
    "scripts": {
        "setup": ".conductor/setup.sh",
        "run": "php artisan horizon",
        "teardown": "FOLDER=$(basename $PWD) && herd unsecure \"${FOLDER}.test\" --silent && herd unlink && mysql -uroot -e \"DROP DATABASE IF EXISTS \\`${FOLDER}\\`\""
    }
}
